[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    safety: scenarios that exist because getting them wrong mis-doses insulin (see docs/TEST-SCENARIOS.md)
    integration: reserved for a later, network-touching pass — nothing marked yet
